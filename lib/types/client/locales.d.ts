/** `settings.background` namespace dictionaries (the Background row's copy). */
/** Simplified Chinese dictionary (the key-set source of truth). */
export declare const zh: {
    'background.title': string;
    'background.upload': string;
    'background.replace': string;
    'background.remove': string;
    'background.preview': string;
    'background.dim': string;
    'upload.tooLarge': string;
    'upload.unsupportedType': string;
    'upload.readError': string;
};
/** The settings.background namespace key union. */
export type BackgroundKey = keyof typeof zh;
/** English dictionary, checked complete against the zh key set. */
export declare const en: {
    'background.title': string;
    'background.upload': string;
    'background.replace': string;
    'background.remove': string;
    'background.preview': string;
    'background.dim': string;
    'upload.tooLarge': string;
    'upload.unsupportedType': string;
    'upload.readError': string;
};
//# sourceMappingURL=locales.d.ts.map